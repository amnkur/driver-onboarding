package com.uber.driver.driver.onboarding.strategy;

import com.uber.driver.driver.onboarding.entity.DocumentCollection;

public class DocumentCollectionStrategy extends OnboardingStrategy<DocumentCollection> {

    public DocumentCollectionStrategy(DocumentCollection documentCollection){
        super(documentCollection);
    }

    @Override
    protected void executeTask(DocumentCollection data) {

    }
}